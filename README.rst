==================
Switchboard Python
==================

TODO - examples of basic usage.


Installation
============

    ./setup.py build
    # Development install
    pip install -e .
    # Actual install
    pip install .
    # To run the tests -- requires a running mail_dispatch server
    ./test_spatch.py
